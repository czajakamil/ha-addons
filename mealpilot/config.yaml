name: "MealPilot"
slug: "mealpilot"
description: "Planowanie posiłków, lista zakupów i batch-cooking (FastAPI + React)."
version: "0.2.0"
url: "https://github.com/czajakamil/ha-addons/tree/main/mealpilot"
init: false

arch:
  - aarch64
  - amd64

startup: application
boot: auto

ingress: true
ingress_port: 8000
panel_icon: "mdi:silverware-fork-knife"
panel_title: "MealPilot"

ports:
  8000/tcp: 8000
ports_description:
  8000/tcp: "MealPilot API + UI"

map:
  - data:rw

options:
  admin_username: "admin"
  admin_password: "changeme123"
  ai_api_url: "https://api.anthropic.com/v1/messages"
  ai_api_key: ""
  ai_model: "claude-haiku-4-5-20251001"
  cors_origins: "*"
  require_cf_access: false
  cookie_secure: false

schema:
  admin_username: str
  admin_password: password
  ai_api_url: str
  ai_api_key: password
  ai_model: str
  cors_origins: str
  require_cf_access: bool
  cookie_secure: bool
